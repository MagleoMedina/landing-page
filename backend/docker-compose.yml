services:
  landing-backend:
    build: .
    container_name: landing-backend
    ports:
      - "8080:8080"
    env_file:
      - ../.env
    # Opcionales: remitente/destinatario distintos
    # environment:
    #   RESEND_FROM: Portfolio <hello@tudominio.com>
    restart: unless-stopped